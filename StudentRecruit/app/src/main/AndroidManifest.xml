<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stud.prathmesh.android.studentrecruit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".reviews.UniRevPage" />
        <activity android:name=".Finder.ListviewFinderUniversities" />
        <activity
            android:name=".loginregister.activityUser.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".loginregister.activityUser.UsersListActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Finder.SchoolFinder1" />
        <activity android:name=".loginregister.activityUser.LoginActivity1" />
    </application>

</manifest>